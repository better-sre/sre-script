# global install:

cnpm install -g webpack
cnpm install -g 

# lib:
cnpm install -g vue-cli
cnpm install -g weex-toolkit
cnpm install -g react-native-cli

cnpm install -g marked


# check es:
cnpm install -g eslint

# tool:
cnpm install -g grunt-cli

# check version:
grunt --version






